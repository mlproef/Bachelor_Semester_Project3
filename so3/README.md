# So3 — two-sided anonymization (stubs)

Intended later step: anonymize **candidates** as well as queries, then rerun ESRC against an anonymized gallery.

Right now `so3/src/pipeline.py` is function stubs only (`comment_body`, `process_candidate_file`, …). There is no CLI and no results directory.

Condition B already writes candidates into `condition_b/anonymized_b/candidates/` (837/1000). Condition C has no candidate side yet.
